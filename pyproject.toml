[project]
name = "ariad"
version = "0.1.0"
description = "The thread through the labyrinth of agentic software work."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mkdocs>=1.6.1",
    "mkdocs-material>=9.5.0",
]

[tool.uv]
package = false
